<%= form_with(model: break_fast, local: true) do |form| %>
  <% if break_fast.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(break_fast.errors.count, "error") %> prohibited this break_fast from being saved:</h2>

      <ul>
        <% break_fast.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item %>
  </div>

  <div class="field">
    <%= form.label :cost %>
    <%= form.number_field :cost %>
  </div>

  <div class="field">
    <%= form.label :restora_id %>
    <%= form.text_field :restora_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
