<%= form_with(model: disrict, local: true) do |form| %>
  <% if disrict.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(disrict.errors.count, "error") %> prohibited this disrict from being saved:</h2>

      <ul>
        <% disrict.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
